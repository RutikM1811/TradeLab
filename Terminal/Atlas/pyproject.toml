[project]
name = "atlas"
version = "0.1.0"
description = "TradeLab AI Engine"
requires-python = ">=3.14"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "pydantic-settings",
    "loguru",
    "httpx"
]